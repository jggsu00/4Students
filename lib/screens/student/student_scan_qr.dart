import 'package:flutter/material.dart';

class StudentScanQR extends StatelessWidget {
  const StudentScanQR({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('scan'),
      ),
    );
  }
}